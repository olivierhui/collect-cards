# 给 Grok 的备注（重启后先读）

用户原话：所有更新都你自己推到 GitHub，不要让我自己点哪个 cmd。

## 必须遵守
- 柜子代码改完后，**由你** `git push origin main`（仓库 olivierhui/collect-cards）。
- **禁止**让用户双击 `推到GitHub.cmd` / `push-github.cmd`。
- 推成功后 Render 会部署 https://collect-cards.onrender.com 。告诉用户「已推、等 Live」，不要丢一个 cmd 给他。

## 内容 vs 代码
- 标题、投放、纠错、拖格子：上线后在网页「编辑页面」改。
- 新功能代码：必须先推 GitHub，Render 才会变。

## 2026-09-17
页面可视化编辑、投放纠错已推：`bbd1ff3` → origin/main。等 Render Live。
